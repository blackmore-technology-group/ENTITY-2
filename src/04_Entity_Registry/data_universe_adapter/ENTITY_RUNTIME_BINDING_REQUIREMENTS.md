# Canonical Runtime Binding — data_universe
Authority: `data_universe`
Canonical owner: `04_Entity_Registry\data_universe_adapter`

- This authority SHALL expose NIKI-safe aggregate data-universe summaries only after the underlying canonical asset, rights, provenance, licensing, usage and economic authorities are qualified.
- It SHALL NOT recreate a monolithic owner field or duplicate authoritative rights/economic state.
- Until dependencies are qualified it SHALL remain unbound and NIKI may use the embedded fallback under the existing resolver rules.

## READY Gate
- Canonical projection is composed from qualified authorities and passes minimum-disclosure tests.
- READY binding pins current `04_Entity_Registry\ENTITY_REQUIREMENTS.md` SHA-256.
